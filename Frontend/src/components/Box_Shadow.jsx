import React from 'react';

const BoxShadow = () => {
  return (
    <div
      style={{
        boxShadow: '0px 4px 8px rgba(0, 0, 0, 0.3)',
        borderRadius: 'xl',
        width: '100%',
        height: '100%',
      }}
    />
  );
};

export default BoxShadow;
